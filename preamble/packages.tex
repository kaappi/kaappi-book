% packages.tex - Package loading for Kaappi book

% Typography
\usepackage[
    protrusion=true,
    expansion=false,
    tracking=false,
    final
]{microtype}
\usepackage{enumitem}
\usepackage{fancyvrb}

% Tables that size themselves to the text block (reference tables in the
% appendices). Column type Y is a ragged-right X column.
\usepackage{tabularx}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}

% Graphics
\usepackage{tikz}
\usetikzlibrary{positioning, shapes.multipart, arrows.meta}
\usepackage{graphicx}
\usepackage{float}

% Boxes and frames
\usepackage[framemethod=tikz]{mdframed}
\usepackage{adjustbox}

% Code listings
\usepackage{listings}

% Cross-references and navigation
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=darkbrown,
    urlcolor=darkbrown,
    bookmarks=true,
    bookmarksnumbered=true,
    pdfauthor={Baiju Muthukadan},
    pdftitle={Kaappi: A Scheme Programming Language},
    pdfsubject={Scheme programming with Kaappi},
}

% Index
\usepackage{imakeidx}
% noautomatic: leave index generation to latexmk/the Makefile (avoids
% imakeidx's restricted shell-escape call failing under -output-directory).
\makeindex[columns=2, title=Index, intoc, noautomatic]

% TOC depth
\setcounter{tocdepth}{2}
\setcounter{secnumdepth}{2}
