# Known divergences between book listings and piped `kaappi` output.
# Format: <file-basename>:<line-of-input>  # reason
#
# Entries marked "upstream" reflect kaappi bugs that make a correct listing
# fail when the checker replays a whole chapter as one session; the listings
# themselves are verified against a fresh REPL. The others are inherent
# limits of replaying listings through a pipe (interactivity, environment).

# --- interactive REPL features (not available in piped mode) ---
ch02-installation.tex:189  # tab completion is an interactive REPL feature
ch02-installation.tex:206  # the _ last-result variable is an interactive REPL feature

# --- session-dependent error suggestion suffix ---
ch08-local-bindings.tex:35  # book shows the stable error prefix; kaappi appends a session-dependent "Did you mean ...?" suggestion

# --- examples that read from stdin or the filesystem ---
ch10-input-and-output.tex:79   # interactive: prompts the user
ch10-input-and-output.tex:80   # interactive: read-line consumes user input
ch10-input-and-output.tex:82   # interactive: depends on user input above
ch10-input-and-output.tex:101  # interactive: read consumes user input
ch10-input-and-output.tex:103  # interactive: depends on user input above
ch10-input-and-output.tex:105  # interactive: depends on user input above
ch10-input-and-output.tex:362  # needs an input.txt file on disk

# --- illustrative code that cannot run (hypothetical procedures) ---
ch14-error-handling.tex:258    # hypothetical http-get/fetch example

# --- upstream kaappi bugs (listings verified in a fresh session) ---
ch16-standard-library.tex:53   # upstream: REPL truncates multiple values (partition)
ch16-standard-library.tex:81   # upstream: REPL truncates multiple values (unzip2)
ch16-standard-library.tex:254  # upstream: mid-session (import (srfi 158)) intermittently fails after heap activity
ch16-standard-library.tex:301  # upstream: mid-session (import (srfi 113) (srfi 128)) intermittently fails after heap activity
ch16-standard-library.tex:302  # upstream: depends on the srfi 113/128 import above
ch16-standard-library.tex:303  # upstream: depends on the srfi 113/128 import above
ch16-standard-library.tex:305  # upstream: depends on the srfi 113/128 import above
ch16-standard-library.tex:307  # upstream: depends on the srfi 113/128 import above
