\chapter{Foreign Function Interface}

Kaappi can call C shared libraries directly from Scheme using the \texttt{(kaappi ffi)} library. This chapter shows how to open shared objects, bind C functions, handle different types, and even pass Scheme callbacks into C code.
