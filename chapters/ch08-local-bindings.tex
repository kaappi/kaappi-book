\chapter{Local Bindings}

Local variables in Scheme are introduced with binding forms: \texttt{let}, \texttt{let*}, \texttt{letrec}, and named \texttt{let}. This chapter explains each form, when to use which, and how named \texttt{let} provides an elegant looping construct without explicit mutation.
